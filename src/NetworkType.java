/**
 * NetworkType is used as field in enum to help inform the Client and Server on how they should interact with
 * NetworkContainer
 */
public enum NetworkType {
    TOKEN, NEWPLAYERREQUEST, NEWPLAYERSEND, NEWTOKEN, DELETETOKEN
}
